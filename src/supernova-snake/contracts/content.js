const { contractStub } = require("./not-implemented");

module.exports = {
  validateContentManifest: contractStub("content.validateContentManifest"),
  spawnPickup: contractStub("content.spawnPickup"),
  extendComboChain: contractStub("content.extendComboChain"),
  triggerReactivePickup: contractStub("content.triggerReactivePickup"),
};
