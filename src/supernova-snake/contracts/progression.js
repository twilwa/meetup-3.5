const { contractStub } = require("./not-implemented");

module.exports = {
  buildSeededRun: contractStub("progression.buildSeededRun"),
  buildDailyRun: contractStub("progression.buildDailyRun"),
  evaluateRunRewards: contractStub("progression.evaluateRunRewards"),
};
