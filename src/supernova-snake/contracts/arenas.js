const { contractStub } = require("./not-implemented");

module.exports = {
  selectBiome: contractStub("arenas.selectBiome"),
  armHazard: contractStub("arenas.armHazard"),
  recordTrailStep: contractStub("arenas.recordTrailStep"),
  advanceArena: contractStub("arenas.advanceArena"),
};
