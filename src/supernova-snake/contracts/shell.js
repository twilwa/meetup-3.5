const { contractStub } = require("./not-implemented");

module.exports = {
  getShellFlow: contractStub("shell.getShellFlow"),
  mapInputPrompt: contractStub("shell.mapInputPrompt"),
  getHudModel: contractStub("shell.getHudModel"),
  buildTutorialBeatPlan: contractStub("shell.buildTutorialBeatPlan"),
};
